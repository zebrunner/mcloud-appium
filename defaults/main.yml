---
#https://androidsdkoffline.blogspot.com/p/android-sdk-tools.html
android_sdk_download_location: https://dl.google.com/android/android-sdk_r24.4.1-linux.tgz
android_sdk_install_location: /opt/android-sdk-linux
android_build_tools: build-tools-29.0.2
android_sdk_versions:
  - 22
  - 23
  - 24
  - 25
  - 26
  - 27
  - 28
  - 29
android_emulator_skins:
  - HVGA
  - QVGA
  - WQVGA400
  - WQVGA432
  - WSVGA
  - WVGA800
  - WVGA854
  - WXGA720
  - WXGA800
  - WXGA800-7in
android_device_types:
  - Tablet
  - Phone
android_device_versions:
  - 5.1.1
  - 6.0.1
  - 6.0
  - 6.0.1
  - 7.0
  - 7.1.2
  - 8.0
  - 8.1
  - 9.0
  - 10.0

docker_device_image_name: appium-device
appium_device_image_version: 1.17
docker_base_emulator_image_name: appium-emulator
docker_tmp_images_path: /tmp


# IMPORTANT: 
# PATCH #1: every time you upgrade appium version please make sute to patch manually roles/appium/templates/system-calls.js to return information about invalid device
# for 1.13.0 add after line 369
# 369 line:
#          _logger.default.debug(`Running '${_this2.executable.path} ${(0, _shellQuote.quote)(args)}'`);
#new one:
#	   const debugInfo = `[[[DEBUG info: ${_this2.executable.path} ${(0, _shellQuote.quote)(args)} --udid ${process.env.DEVICEUDID} --name ${process.env.DEVICENAME}]]]`;
# and 6-7 lines below return updated value
#          return stdout;
# change to
#          return stdout + '\n' + debugInfo;


appium_install_location: /opt/appium
appium_version: 1.15.0

opencv_install_location: /opt
opencv_version: 4.15.0

selenoid_session_timeout_sec: 300
selenoid_port: 4445

grid_router_location: /etc/grid-router
grid_router_users:
  - { login: user1, password: user1 }
  - { login: user2, password: user2 }

selenium_hub_port: 4444

nginx_home: /opt/nginx
ssl_crt: ssl.crt
ssl_key: ssl.key

# to fix "FATAL ERROR: CALL_AND_RETRY_LAST Allocation failed - JavaScript heap out of memory" during video recording
max_old_space_size_value: 8172
